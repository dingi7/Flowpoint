# Starting Point Repository

This repository serves as the starting point for our projects that include a Firebase backend and a Vite-based front-end.

## Getting Started
1. Install dependencies for front-end: `cd front-end && npm install`
2. Install dependencies for functions: `cd functions && npm install`